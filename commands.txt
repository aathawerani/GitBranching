docker exec -it gitlab-local bash
gitlab-rails console
User.all.each { |u| puts "#{u.id} | #{u.username} | #{u.email} | admin=#{u.admin}" }
u = User.find_by_username('root')
u.password = "Admin123!"
u.password_confirmation = "Admin123!"
u.confirmed_at = Time.now
u.save!
exit
login in to http://localhost:8080
username: root
password: Admin123!
cd /workspace/java-sample-app
git init
git remote add origin http://localhost:8080/root/my-app.git
git config --global user.name "root"
git config --global user.email "root@example.com"
ssh-keygen -t ed25519 -C "root@gitlab"
cat ~/.ssh/id_ed25519.pub
ssh -T git@localhost -p 22
git remote set-url origin ssh://git@localhost:22/root/my-app.git
git remote -v
git push -u origin master
git checkout -b main
git push -u origin main
git checkout -b release/1.0.0
git push -u origin release/1.0.0
git tag -a v1.0.0 -m "Initial product release"
git push origin v1.0.0
git checkout main
git pull
git checkout -b feature/personalized-greeting
git add .
git commit -m "Added personalized greeting feature"
git push -u origin feature/personalized-greeting
git checkout main
git merge feature/personalized-greeting --no-ff
git push origin main
